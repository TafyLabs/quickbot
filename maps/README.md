# Maps

Nav2 occupancy maps live here as `<name>.yaml` + `<name>.pgm` pairs from `nav2_map_server map_saver_cli`.

Bring-up target: `quickbot_lab.{yaml,pgm}` — saved in **P5** (Gate G7) via the procedure in [docs/runbook.md](../docs/runbook.md#mapping-gate-g7).
